{
	"name": "secrets-to-env",
	"version": "1.0.0",
	"description": "Convert secrets to environment variables in GitHub Actions",
	"main": "dist/index.js",
	"scripts": {
		"build": "tsc && ncc build --license licenses.txt"
	},
	"dependencies": {
		"@actions/core": "^1.10.1"
	},
	"devDependencies": {
		"@types/node": "^22.1.0",
		"@vercel/ncc": "^0.38.1",
		"prettier": "^3.3.3",
		"typescript": "^5.5.4"
	}
}